import MainNav from './MainNav'
import SideNav from './SideNav'

const Onboarding = ( ) => {
  /*
  This will be the first thing the user see that guides them through the system. It can be replaced with what anthony has for the skip signup right now.
  The details are in the UI link that's shown, and it's nothing "new" just stepping through the system so it shouldn't be super duper hard
   */
  return (
    <div>
      <MainNav />
      <SideNav />
    </div>
  )
}

export default Onboarding
